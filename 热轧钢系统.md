1.查找相关资料，把资料给GPT，深度研究报告给我一个项目的基本方案
![[Pasted image 20260814100759.png]]

![[补充完善.docx]]
2.大致查看报告，把自己目前不需要的内容叫codex忽略

![[Pasted image 20260814101146.png]]![[Pasted image 20260814101225.png]]![[Pasted image 20260814101306.png]]检查系统缺陷，比如，网页中的功能不能使用，没用用户登陆界面，摄像头功能不能调用外接摄像头和本地摄像头，继续叫codex修复
![[Pasted image 20260814102043.png]]途中，发现这个系统并没有按照前后端进行分类无法在终端上运行
继续完善---进行前后端的分类---
在终端运行
    前端   --cd 前端项目所在目录
	    npm install
	    npm run dev
    后端   --cd 后端项目所在目录
		python -m venv .venv
		.\.venv\Scripts\Activate.ps1
		pip install -r requirements.txt
		python app.py
前后端分别在独立的终端上运行

进行文档".md"的编写
主要进行“系统架构”+“接口说明”，编写完成询问"这两个文档能不能完成开发任务需求"
端口组成“协议+主机+路径+端口”













上传项目到Gitgub
使用git 命令
git init-----初始化
git status---查看当前状态
git add-----加入文件到Git暂存区
git commit-m "说明" ---备注
git branch-M main----把默认分支改为main
git remote add origin https://github.com/你的用户名/steel-ai-inspection.git    -----连接至Git项目的远程仓库
git push -u origin main-----上传至Github
git remote -v ------查看远程仓
（origin--原来远程仓库   GitHub--Github上的远程仓库）![[Pasted image 20260814103936.png]]
git push -u github main ----当前项目上传至Github

修改作者
git config user.name "你的名字"
git config user.email "你的GitHub绑定邮箱"
git commit --amend --reset-author --no-edit
git push --force-with-lease github main 更新

修改commit
git rebase --abort
git rebase -i --root
`pick` 改成 reword
输入 i 进行编辑
Esc-->:wq-->Enter
git push --force-with-lease github main 更新